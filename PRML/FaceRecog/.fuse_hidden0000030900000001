#coding=gbk
import numpy as np
import matplotlib.pyplot as plt

x = y = np.arange(-4, 4, 0.1)
x, y = np.meshgrid(x,y)
plt.contour(x, y, x**2 + y**2, [1])     #x**2 + y**2 = 1 的圆形

plt.axis('scaled')
plt.show()
